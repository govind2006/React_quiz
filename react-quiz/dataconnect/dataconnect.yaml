specVersion: "v1beta"
serviceId: "react-quiz"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "react-quiz-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
